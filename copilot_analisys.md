# AI-Marketing Platform — Copilot Analysis

> Generated by GitHub Copilot CLI  
> Project: `AI-Marketing-New`  
> Path: `C:\AI Marketing Git\AI-Marketing-New`

---

## 1. Project Overview

**AI-Marketing New** is an enterprise SaaS platform for orchestrating AI-driven marketing campaigns across Facebook, TikTok, Instagram, and YouTube. It features role-based access control (RBAC), multi-step AI-assisted campaign strategy, creative asset management, and integrated ad deployment.

### Technology Stack

| Layer        | Technology                                        |
|--------------|--------------------------------------------------|
| Frontend     | Vanilla JS SPA, Vite 7.2.4, Tailwind CSS 4.x    |
| Backend      | ASP.NET Core 9.0 (Web API)                       |
| Database     | PostgreSQL + Entity Framework Core 9.0            |
| Auth         | JWT Bearer Tokens (HS256, 8hr expiry)             |
| AI Engine    | Google Gemini 2.0 Flash API                       |
| Ad Platforms | Facebook Graph API v19.0, TikTok Business API v1.3|
| Hashing      | BCrypt.NET                                        |
| Deployment   | Docker Compose (3 containers: db, backend, frontend)|

---

## 2. System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         DOCKER COMPOSE STACK                        │
│                                                                     │
│   ┌──────────────┐     ┌──────────────────┐     ┌───────────────┐  │
│   │   FRONTEND   │     │     BACKEND      │     │  POSTGRESQL   │  │
│   │  Nginx :80   │────▶│  ASP.NET :5243   │────▶│  Postgres:16  │  │
│   │  Vite SPA    │     │  REST API        │     │  DB:MarketingAI│ │
│   │  main.js     │◀────│  Program.cs      │◀────│               │  │
│   │  (19+ screens│     │  984 lines       │     │  7 tables     │  │
│   │   3277 lines)│     │                  │     │               │  │
│   └──────────────┘     └────────┬─────────┘     └───────────────┘  │
│                                 │                                   │
│                    ┌────────────┼────────────┐                      │
│                    ▼            ▼            ▼                      │
│           ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│           │  Google  │  │ Facebook │  │  TikTok  │                 │
│           │  Gemini  │  │ Graph API│  │ Biz API  │                 │
│           │  2.0     │  │  v19.0   │  │   v1.3   │                 │
│           └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. Data Flow Diagram

### Level 0 — Context Diagram

```
                                          ┌──────────────────┐
  ┌──────────────┐  Campaign Request      │                  │
  │  Marketing   │ ─────────────────────▶ │  AI-Marketing    │
  │  Team Users  │  Reports & Metrics     │  Platform        │
  │  (4 Roles)   │ ◀───────────────────── │                  │
  └──────────────┘                        └────────┬─────────┘
                                                   │
                          ┌────────────────────────┼───────────────┐
                          ▼                        ▼               ▼
                  ┌──────────────┐      ┌──────────────┐  ┌──────────────┐
                  │ Google Gemini│      │ Facebook     │  │ TikTok       │
                  │ API          │      │ Graph API    │  │ Business API │
                  │ (AI Engine)  │      │ (Ad Platform)│  │ (Ad Platform)│
                  └──────────────┘      └──────────────┘  └──────────────┘
```

### Level 1 — Internal System Data Flow

```
┌══════════════════════════════════════════════════════════════════════╗
║                       AI-MARKETING PLATFORM                         ║
╠══════════════════════════════════════════════════════════════════════╣
║                                                                      ║
║  ┌──────────┐  Credentials   ┌──────────────────┐                   ║
║  │  LOGIN   │ ─────────────▶ │   AUTH SERVICE   │                   ║
║  │  SCREEN  │ ◀───────────── │  JWT + BCrypt    │                   ║
║  └──────────┘  JWT Token     └────────┬─────────┘                   ║
║       │                               │                              ║
║       │ Token                    Read/Write                          ║
║       ▼                               ▼                              ║
║  ┌──────────┐  API Requests   ┌──────────────────┐                  ║
║  │ FRONTEND │ ◀─────────────▶ │   POSTGRESQL DB  │                  ║
║  │ SPA      │  JSON Responses │  - users         │                  ║
║  │ main.js  │                 │  - roles         │                  ║
║  └────┬─────┘                 │  - screens       │                  ║
║       │                       │  - role_screens  │                  ║
║       ├──────────┐            │  - campaigns     │                  ║
║       ▼          ▼            │  - brand_guide   │                  ║
║  ┌─────────┐ ┌─────────┐     │  - cmo_queue     │                  ║
║  │ GEMINI  │ │ DEPLOY  │     └──────────────────┘                  ║
║  │ SERVICE │ │ SERVICE │      ┌──────────────────┐                  ║
║  └────┬────┘ └────┬────┘      │  FILE STORAGE    │                  ║
║       │           │           │  ppc_queue.json  │                  ║
║       ▼           ▼           │  /Assets/        │                  ║
║  ┌─────────┐ ┌──────────┐     │  /Assets Library/│                  ║
║  │ Google  │ │Facebook +│     └──────────────────┘                  ║
║  │ Gemini  │ │TikTok API│                                           ║
║  └─────────┘ └──────────┘                                           ║
╚══════════════════════════════════════════════════════════════════════╝
```

---

## 4. System Flow — Campaign Lifecycle

```
START
  │
  ▼
[EXPERT: Select Objective] ──────────── Reach / Click / Sell
  │
  ▼
[EXPERT: Define Target Audience]
  │  Country, Language, Age Range, Gender
  │  (Multiple targeting sets allowed)
  │
  ▼
[EXPERT: Strategy Hub — Phase 1]
  │  Enter campaign brief
  │  POST /api/gemini/questions ──▶ Google Gemini API
  │  ◀── 5 Level-1 Conceptual Questions
  │
  ▼
[EXPERT: Strategy Hub — Phase 2]
  │  Answer Level-1 questions
  │  POST /api/gemini/follow-up ──▶ Google Gemini API
  │  ◀── 5 Level-2 Psychological Questions
  │
  ▼
[EXPERT: Strategy Hub — Phase 3]
  │  Answer Level-2 questions
  │  Full strategy profile complete (stored in client state)
  │
  ▼
[EXPERT: Creative Config]
  │  Select style: Cinematic / Minimalism / Cyberpunk / Vintage
  │  Select ratio: 1:1 / 16:9 / 9:16
  │  Set counts: photos / videos / posts
  │  POST /api/campaigns ──▶ Save to PostgreSQL
  │  Download sample variations from Unsplash ──▶ /Assets folder
  │
  ▼
[EXPERT: Creative Studio]
  │  Review assets in /Assets
  │  Approve selected assets
  │  POST /api/assets/approve ──▶ Copy to /Assets Library
  │  POST /api/cmo/queue ──▶ Add to CMO Queue (PostgreSQL)
  │
  ▼
[BUSINESS ADMIN: Ad Approvals]
  │  GET /api/cmo/queue ──▶ Fetch pending assets
  │  Toggle Approve / Reject per asset
  │  Approved: POST /api/ppc/queue ──▶ Write to ppc_queue.json
  │
  ▼
[PPC SPECIALIST: Approved Assets]
  │  GET /api/ppc/queue ──▶ Read ppc_queue.json
  │  Select assets for deployment
  │
  ▼
[PPC SPECIALIST: Platform Selection & Deploy]
  │
  ├──────────────────────────────────┐
  ▼                                  ▼
[Facebook Deploy]              [TikTok Deploy]
  │  POST /api/deploy/facebook       │  POST /api/deploy/tiktok-adgroup
  │  1. Upload media (image/video)   │  1. Create Ad Group
  │  2. Create Campaign              │     (budget, placement, targeting)
  │  3. Create Ad Set                │  2. Create Ad Creative
  │  4. Create Ad Creative           │     (video_id, text, CTA)
  │  5. Create Ad                    │  Return adgroup_id + ad_id
  │  Return campaign/ad IDs          │
  └──────────────────┬───────────────┘
                     │
                     ▼
          [MONITORING DASHBOARD]
            Active Ads count
            Total Spend tracking
            Efficiency metrics
            Auto-actions: Scale Up / Monitor /
                          Shut Down / Budget Realloc

END
```

---

## 5. Authentication Flow

```
CLIENT                        SERVER                        DATABASE
  │                              │                               │
  │  POST /api/auth/login        │                               │
  │  { username, password }      │                               │
  │─────────────────────────────▶│                               │
  │                              │  SELECT user WHERE            │
  │                              │  username = ?                 │
  │                              │──────────────────────────────▶│
  │                              │◀── User record ───────────────│
  │                              │                               │
  │                              │  BCrypt.Verify(password,hash) │
  │                              │                               │
  │                              │  SELECT role + screens        │
  │                              │  for user.role_id             │
  │                              │──────────────────────────────▶│
  │                              │◀── Role + Screen list ─────────│
  │                              │                               │
  │                              │  Generate JWT (HS256, 8hr)    │
  │                              │  { name, role, sub, iss,      │
  │                              │    aud, exp }                 │
  │                              │                               │
  │◀─────────────────────────────│                               │
  │  { token, user: {            │                               │
  │    username, role, email,    │                               │
  │    screens[] } }             │                               │
  │                              │                               │
  │  Store in localStorage       │                               │
  │  Render role-specific screens│                               │
```

---

## 6. User Roles & Permissions

| Role              | Key Screens & Capabilities |
|-------------------|---------------------------|
| **Admin**         | Platform Config (API keys), Company Profile, User Management, Role Management, Brand Guidelines, Creative Assets, Global Calendar |
| **Expert**        | Campaign Objective, Target Audience, Strategy Hub (3-phase AI), Creative Config, Creative Studio |
| **Business Admin**| Budget & Matrix, Ad Approvals (CMO Queue), AI Monitoring, Ad Performance, Budget Overview |
| **PPC Specialist**| Approved Assets (PPC Queue), Platform Selection, Deploy (Facebook + TikTok), Ad Performance, AI Monitoring |

---

## 7. Database Schema

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    users     │       │    roles     │       │   screens    │
├──────────────┤  FK   ├──────────────┤       ├──────────────┤
│ PK id        │──────▶│ PK id        │       │ PK id        │
│    username  │       │    name      │       │    name      │
│    password  │       └──────┬───────┘       │    display   │
│    email     │              │               └──────┬───────┘
│ FK role_id   │              ▼                      │
└──────────────┘       ┌──────────────┐              │
                       │ role_screens │◀─────────────┘
                       ├──────────────┤
                       │ FK role_id   │
                       │ FK screen_id │
                       └──────────────┘

┌───────────────────┐       ┌──────────────┐       ┌──────────────┐
│  brand_guidelines │       │  campaigns   │       │  cmo_queue   │
├───────────────────┤       ├──────────────┤       ├──────────────┤
│ PK id             │       │ PK id        │       │ PK id        │
│    brand_label    │       │    brief     │       │    url       │
│    tone           │       │    style     │       │    title     │
│    language       │       │    ratio     │       │    type      │
│    whitelist      │       │    timestamp │       │    status    │
│    blacklist      │       │    status    │       │    added_at  │
│    typography JSONB│      └──────────────┘       └──────────────┘
│    palette array  │
│    updated_at     │       ┌──────────────────────────────────────┐
└───────────────────┘       │  FILE STORAGE (JSON + Filesystem)    │
                            ├──────────────────────────────────────┤
                            │  ppc_queue.json  — PPC dispatch queue│
                            │  /Assets/        — Working assets    │
                            │  /Assets Library/— Approved assets   │
                            └──────────────────────────────────────┘
```

---

## 8. API Endpoint Map

```
/api
  ├── /auth
  │     ├── POST /register          Create user account
  │     └── POST /login             Authenticate → JWT + screens
  │
  ├── /rbac
  │     ├── GET    /roles           List all roles
  │     ├── POST   /roles           Create role
  │     ├── PUT    /roles/{id}      Update role
  │     ├── DELETE /roles/{id}      Delete role
  │     ├── GET    /screens         List all screens
  │     ├── GET    /users           List users with roles
  │     ├── DELETE /users/{id}      Revoke user
  │     ├── GET    /role-permissions/{roleId}
  │     ├── POST   /role-permissions
  │     └── POST   /seed            Initialize default data
  │
  ├── /guidelines
  │     ├── GET  /                  Fetch latest brand guideline
  │     └── POST /                  Save brand guideline
  │
  ├── /campaigns
  │     ├── GET  /                  List all campaigns
  │     └── POST /                  Create campaign
  │
  ├── /assets
  │     ├── GET    /                List /Assets files
  │     ├── DELETE /{filename}      Delete asset
  │     ├── POST   /save-url        Download image from URL
  │     └── POST   /approve         Copy to /Assets Library
  │
  ├── /assets-library
  │     └── GET  /                  List /Assets Library files
  │
  ├── /cmo
  │     ├── GET  /queue             Get CMO approval queue
  │     └── POST /queue             Update CMO queue
  │
  ├── /ppc
  │     ├── GET  /queue             Get PPC dispatch queue
  │     └── POST /queue             Update PPC queue
  │
  ├── /gemini
  │     ├── POST /questions         Generate Level-1 AI questions
  │     └── POST /follow-up         Generate Level-2 AI questions
  │
  ├── /deploy
  │     ├── POST /facebook          Deploy ad to Facebook
  │     └── POST /tiktok-adgroup    Deploy ad to TikTok
  │
  └── /test
        └── GET  /facebook-connection   Test Facebook API connection
```

---

## 9. Asset Pipeline Flow

```
Expert uploads/generates assets
         │
         ▼
  [Save to /Assets]  ◀── POST /api/assets/save-url
         │
         ▼
  [Expert reviews in Creative Studio]
         │
    Approve selected
         │
         ▼
  [Copy to /Assets Library] ◀── POST /api/assets/approve
         │
         ▼
  [Add to CMO Queue (PostgreSQL)] ◀── POST /api/cmo/queue
         │
         ▼
  [Business Admin reviews]
    Approve / Reject
         │
    If Approved
         │
         ▼
  [Write to ppc_queue.json] ◀── POST /api/ppc/queue
         │
         ▼
  [PPC selects & deploys]
         │
         ▼
  [Facebook Graph API + TikTok Business API]
```

---

## 10. AI Strategy Flow (Gemini)

```
Campaign Brief Input
         │
         ▼
  [Hash brief & check cache]
         │
    ┌────┴────┐
  Cache HIT  Cache MISS
    │           │
    │           ▼
    │   [POST to Gemini API]
    │   model: gemini-2.0-flash
    │           │
    │           ▼
    │   [Parse 5 Level-1 Questions]
    │           │
    │   [Store in cache]
    │           │
    └────┬──────┘
         ▼
  Return questions to frontend
         │
         ▼
  User answers Level-1 Questions
         │
         ▼
  [POST /api/gemini/follow-up]
  (brief + L1 answers → Gemini)
         │
         ▼
  5 Level-2 Psychological Questions
         │
         ▼
  User answers Level-2 Questions
         │
         ▼
  Full Strategy Profile Complete
  (stored in client-side state)
```

---

## 11. Deployment Architecture (Docker Compose)

```
Host Machine
  │
  ├── :80  ──▶  frontend (Nginx + Vite SPA)
  │               │
  │               │ proxy /api/*
  │               ▼
  ├── :5243 ──▶  backend (ASP.NET Core 9.0)
  │               │
  │               │ EF Core ORM
  │               ▼
  └── :5432 ──▶  db (PostgreSQL 16-alpine)
                  │
                  └── Volume: pgdata (persistent)

Volumes:
  pgdata    — PostgreSQL data persistence
  assets    — /app/Assets (working files)
  library   — /app/Assets Library (approved files)
```

---

## 12. Key Backend Services

| Service                    | Responsibility |
|----------------------------|----------------|
| `FacebookAdsService.cs`    | Media upload, campaign/adset/creative/ad creation via Facebook Graph API |
| `DeploymentOrchestrator.cs`| Coordinates multi-step ad deployment across platforms |
| `BrandComplianceService.cs`| Enforces brand guidelines on creative assets |
| `MetricsFetchService.cs`   | Fetches ad performance metrics |
| `GoogleAdsService.cs`      | Google Ads integration (planned) |
| `YouTubeAdsService.cs`     | YouTube Ads integration (planned) |

---

*End of Copilot Analysis*
